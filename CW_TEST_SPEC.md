# 货币战争测试规范（CW_TEST_SPEC）

> 定位：`README.md`「测试纪律」是**全测试仓单一源**（20+1 条硬规则），本文档只补
> 货币战争（CW）玩法专属层——域划分、各域合同形态、真帧锚细则、先删后重建范式。
> 冲突时以 README 为准；本文档的变更随对应域的重建批走。

## 1. 域划分与合同形态

CW 测试按被测层分七域，每域有固定合同形态。新测试先判域，再按域形态写；
判不了域的按机制找主题文件，禁新建 round/工作项命名文件（README 第 17 条）。

| 域 | 被测层 | 合同形态 | 现役代表 |
|---|---|---|---|
| obs | 截图→结构化事实 | 六类：解析真值对照 / 安全默认语义 / 对账仲裁 / 布局选档 / 真实图片识别锚 / 入口契约 | `test_cw_obs_parse_facts` 等 6 件 |
| data | 静态数据表 | 四种：引用完整性 / 值域守恒 / 派生一致性 / 登记门 | `test_cw_data_tables` |
| kernel 判策 | 决策规则 | 行为锁+锁语义重推；能数学证明的用证明集推导锚定 | 各机制主题件 |
| strategies | 策略行为 | 策略开关生命周期门 + A/B 对拍（申报规范见 README G2 节） | `test_cw_sell_*` 族 |
| sim | 模拟引擎/裁判 | 锁形状不锁分布；n 取断言成立的最小值（README 第 12 条） | `test_cw_sim_*` 族 |
| ops | op 流程 | `fixture_controller` harness + `fast_sleep()`（README 第 5/6 条） | `test_cw_entry_flow` |
| telemetry | 录制/回放/归档 | 读点链组装锁 + 归档判读（对账兜底优先，与实机档案对读） | `test_cw_telemetry_*` 族 |

## 2. 真帧锚细则（README 第 21 条在 CW 的落地）

1. **来源**：真实截图帧——优先复用 `sr-od-test/screens/<画面名>/`、
   `fixtures/`、`fixtures_settle/` 既有帧；新帧从实机取证帧（`.debug/`
   evidence，易失）裁剪入库。
2. **真值确认三方式**（注释必须注明用哪种）：看图人工核对 / 用户裁决 /
   旧测试已登记真值复跑核对。真值有分歧（识别输出与看图不一致）→ 不锁，
   挂账待裁决，禁闭眼写死（判例：花火星级，用户裁决后锁定）。
3. **一图一测**：同一张图的全部检查面聚合在一个测试里——一次读取、一次识别、
   逐条断言。同一帧被不同域的函数消费（如 prep_stall 帧的格数与身份）按域
   分文件，但同域内禁止多处读同一张图重新识别。
4. **体积**：裁剪到识别所需最小区域；整帧仅限多区域联动验证必需
   （如 19 槽身份校验）；PNG 无损。
5. **禁合成图片**（手画色块/贴图拼装）：合成通过 ≠ 实机能识别。数值/文本
   语料合成不受此限（README 第 19 条）。

## 3. 不写什么（兜底边界）

以下由实机对账/架构冗余兜底，测试不重复保险（判据：架构冗余已兜的地方，
单点再养一层测试是不必要的重复保险）：

1. **OCR 引擎本身**与阈值标定值（`_CV_SLOT_STD_MIN`、Hu 距离、对称比等）
   ——真帧锚锁「这张图识别出这个结果」，阈值漂移在帧上自然红，标定值不进断言；
2. **单点检测器的管线冒烟**——board 三票、布局三信号、部署计数对账、星级
   费用对账等多信号冗余已兜单点故障；流程卡死有 no_progress_guard 哨兵；
3. **区域矩形正确性**——screen_info 是坐标单一真相源，锁坐标反而挡建档；
4. **分布数值**——sim 裁判域锁形状不锁分布（README 第 11 条）；
5. **逐条目数据断言**——数据表用登记门替代（见 §4）。

## 4. data 域四种合同

静态数据表按「文件 × 合同」配测试，禁止逐条目铺（72 角色/98 装备逐条断言
= 快照陷阱：改数据必红且无可登记语义）。

1. **引用完整性**：外键全在注册表——CHARACTERS.factions ⊆ FACTIONS、
   SYNTHESIS 派生集 ⊆ EQUIPMENT_ROSTER、BOSS_NICKNAMES 值 ⊆ BOSS_MECHANICS
   （俗称/规范名名字空间对齐——错位=匹配永不命中，历史病灶防复发）、
   recipe 名 ∈ EQUIPMENT_ROSTER；
2. **值域守恒**：REFRESH_PROB 每行概率和=1、副本上限随费用非增、胜负率
   ∈(0,1)、投资注册表 id/name 唯一；
3. **派生一致性**：import 时派生的常量（synthesis 族）与从单一源重算对照
   （防派生表达式被改手抄）；生成器产物（plaza_posts/plaza_comps）用
   快照对拍守卫各 1 条，禁更多；
4. **登记门**：覆盖完整性断言（每费用档非空/每键域有条目）——红时知道
   该登记什么语义，允许高频被动更新（README 第 7 条登记门形态）。

## 5. 先删后重建范式

整族测试「自然增殖」失控时的重建流程（试点判例 = obs 识别层）：

1. **规格先行**：删之前立「该测什么」的边界文档（输入形状→输出→拒绝口径），
   重建只从规格+源码推导，禁参考旧测试（防语义继承）；
2. **新合同全绿**：按域合同形态写，跑绿；
3. **删除名单逐文件主体判定**：禁按文件名归桶盲删——混杂体（多域语义
   混住）留待主题归并，判不清的不删；
4. **迁移镜像义务**：每删一文件先答「哪几条是独家语义、迁去哪个主题文件」
   ——迁移落点按域归一，同帧检查面聚合到已跑该帧的测试；
5. **整批删旧 → 全量回归 → 逐文件点名提交**；
6. **待删除标记**：暂不删的文件在模块 docstring 后插标记块（已被替代→
   去向 / 待归并→去向 / 删除前置条件），任何读到该文件的人可见其退役计划；
   并行批在飞的文件禁插标记（同文件禁两批同时持有编辑），挂账待落地后补。

## 6. 并行共存（CW 特有风险面）

- CW 是多批并行活跃区（策略迭代/状态迁移/识别演进同时在飞）：
  编辑任何文件前 `git status` 查在飞改动，src 半态期（SyntaxError 收集红）
  等待自愈复跑，禁立即「修复」；
- 被并行批未提交改动覆盖的 src 模块，其测试**挂起不派**——锁流动中的
  行为=白锁（判例：telemetry 族因 `cw_performance` 在飞整族挂起）；
- 模型目录瞬态（预检守卫开发期反复移动 `assets/models/onnx_ocr`）：
  等待自愈（实测 <90s 闭合），禁立即恢复下载互踩。
